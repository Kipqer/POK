#include <stdlib.h>
#include <math.h>
#include <stdio.h>
#include <time.h>

int main(){

	char znak[1000]={0}, slowo[1000];
	int i,j,k[200]={0};
	
	printf("Wprowadz slowo: ");
	scanf("%s", slowo);
	printf(slowo);
	
	
/*	for(i=0;;i++){
		scanf("%c",&znak[i]);
		if(znak[i]==10)break;
		
		for(j=97;j<=122;j++)
		{
			if(char(j)==znak[i]){
				k[j]++;
			}
		}
	}
	printf("Ilosc liter: %d",i);
*/ 	return 0;
}

