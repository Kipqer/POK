#include <stdlib.h>
#include <math.h>
#include <stdio.h>

int N=255;
char tab[255];

char odwroc(char tab[N]){
	
}

int main(){
	
	FILE *plik=fopen("slowo.txt","r");
	fscanf(plik,"%c",tab[N]);
	odwroc(tab);
	
	return 0;
}

